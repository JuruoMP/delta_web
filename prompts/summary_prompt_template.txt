# Analyst-Bot Task

## 1. INSTRUCTIONS
**CRITICAL: Your response must be a single, valid JSON object with NO explanatory text outside of the JSON structure. Any text outside the JSON object will cause parsing errors.**

Analyze the conversation transcript provided below in the `CONVERSATION_DATA` section. Extract the specified information and return it as a single, valid JSON object that strictly adheres to the provided schema.

## 2. CONVERSATION_DATA
```json
{{json_str}}
```

## 3. REQUIRED_OUTPUT_JSON_SCHEMA
You MUST populate the following JSON schema exactly as defined, with all fields present even if their value is an empty list or string:
```json
{
  "summary": "string",
  "topics": [
    {
      "title": "string",
      "summary": "string",
      "information": "string",
      "type": "string",
      "sentiment": "string"
    }
  ],
  "action_items": [
    {
      "owner": "string",
      "task": "string",
      "due_date": "string or null"
    }
  ],
  "key_decisions": ["string"],
  "named_entities": {
    "people": ["string"],
    "organizations": ["string"],
    "locations": ["string"],
    "projects": ["string"],
    "dates_times": ["string"]
  }
}
```

## 4. EXTRACTION GUIDELINES
- summary: A concise overview of the entire conversation (50-100 characters)
- topics: Extract ONLY the 3-5 MOST IMPORTANT discussion topics with NO chit-chat content. Focus on preserving comprehensive important information:
  - title: Brief topic heading
  - summary: A detailed paragraph that comprehensively captures and records all key information and important details about this topic, including main ideas, opinions, discussions, and developments
  - information: Specific details, facts or data (ensure comprehensive coverage of important information)
  - type: One of: "工作事务", "家庭生活", "学习研究", "社交娱乐", "健康医疗", "金融理财", "其他" (EXCLUDE "日常闲聊")
  - sentiment: Overall sentiment: "积极", "中性", or "消极"

## 5. FORMAT ENFORCEMENT
- **DO NOT ADD ANY TEXT BEFORE OR AFTER THE JSON OBJECT**
- Ensure all JSON string values are properly escaped
- Ensure the JSON is syntactically correct with proper opening/closing brackets
- Verify that all fields required by the schema are present

## 6. EXAMPLE OUTPUT
```json
{
  "summary": "项目进度讨论和下一步计划",
  "topics": [
    {
      "title": "项目A开发进度",
      "summary": "讨论了项目A的当前开发进度，包括前端界面优化和后端API实现情况",
      "information": "前端已完成80%，后端API已完成60%，预计下周可以完成第一轮测试",
      "type": "工作事务",
      "sentiment": "积极"
    }
  ],
  "action_items": [
    {
      "owner": "张三",
      "task": "完成前端界面优化",
      "due_date": "2023-06-15"
    }
  ],
  "key_decisions": ["项目A延期至7月初发布"],
  "named_entities": {
    "people": ["张三"],
    "organizations": [],
    "locations": [],
    "projects": ["项目A"],
    "dates_times": ["2023-06-15", "7月初"]
  }
}
```
  - IMPORTANT: Topics MUST be sorted in descending order of importance based on conversation focus and significance
- action_items: Extract ONLY the 3-5 MOST IMPORTANT future tasks or commitments THAT REQUIRE THE USER TO COMPLETE with:
  - owner: Person responsible (if not specified, attribute to relevant speaker)
  - task: Task description, MUST be listed in bullet points and use concise language
  - due_date: Deadline (null if not specified)
- key_decisions: Extract all explicit agreements or conclusions
- named_entities: Extract all named entities into appropriate categories